
# 电商商品价格自动化采集与对比工具 - 架构设计

## 1. 整体架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        应用层 (Application Layer)                       │
│  ┌─────────────────────┐  ┌─────────────────────────────────────────┐  │
│  │   命令行工具 CLI     │  │           演示网页 Web UI              │  │
│  │   (cli.py)          │  │         (app.py + templates/)          │  │
│  └─────────────────────┘  └─────────────────────────────────────────┘  │
└───────────────────────────────┬─────────────────────────────────────────┘
                                │
┌───────────────────────────────▼─────────────────────────────────────────┐
│                        业务逻辑层 (Service Layer)                       │
│  ┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────┐  │
│  │  CrawlerService     │  │  DataProcessor      │  │  Visualizer     │  │
│  │  (爬虫服务)         │  │  (数据处理服务)     │  │  (可视化服务)   │  │
│  │  - 抖音商品搜索     │  │  - 数据清洗去重     │  │  - 价格趋势图   │  │
│  │  - 商品详情抓取     │  │  - 价格排序         │  │  - 对比表格     │  │
│  │  - 数据存储         │  │  - 性价比分析       │  │  - 推荐标注     │  │
│  └─────────────────────┘  └─────────────────────┘  └─────────────────┘  │
└───────────────────────────────┬─────────────────────────────────────────┘
                                │
┌───────────────────────────────▼─────────────────────────────────────────┐
│                        数据访问层 (Data Layer)                          │
│  ┌─────────────────────┐  ┌─────────────────────┐  ┌─────────────────┐  │
│  │   API Client        │  │    File Storage     │  │  In-memory DB   │  │
│  │  (api_client.py)    │  │   (data/*.json)     │  │  (临时缓存)     │  │
│  └─────────────────────┘  └─────────────────────┘  └─────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
```

## 2. 模块划分

| 模块 | 文件 | 职责 |
|------|------|------|
| 命令行入口 | `cli.py` | 处理命令行参数，启动采集流程 |
| Web 服务 | `app.py` | 提供演示网页和 API 接口 |
| 爬虫服务 | `crawler/douyin_crawler.py` | 抖音平台数据采集核心逻辑 |
| API 客户端 | `crawler/api_client.py` | HTTP 请求封装 |
| 数据处理 | `processor/data_processor.py` | 数据清洗、去重、排序 |
| 可视化 | `visualizer/chart_generator.py` | 图表生成与渲染 |
| 配置 | `config/settings.py` | 全局配置参数 |
| 工具 | `utils/helpers.py` | 通用辅助函数 |
| 数据模型 | `models/product.py` | 商品数据结构定义 |

## 3. 核心数据结构

```python
class Product:
    id: str              # 商品ID
    name: str            # 商品名称
    price: float         # 价格
    sales: int           # 销量
    shop_name: str       # 店铺名称
    shop_rating: float   # 店铺评分 (0-5)
    url: str             # 商品链接
    image_url: str       # 商品图片
    crawl_time: datetime # 采集时间
```

## 4. 数据采集流程

```
用户输入关键词 → 构建搜索请求 → 发送HTTP请求 → 解析响应JSON → 提取商品列表 → 
逐个获取商品详情 → 数据清洗去重 → 按价格排序 → 生成可视化图表 → 输出结果
```

## 5. 数据处理流程

1. **数据清洗**：
   - 去除无效数据（价格为0、销量为空等）
   - 统一价格格式（去除非数字字符）
   - 处理缺失值

2. **去重逻辑**：
   - 按商品ID去重
   - 按商品名称+价格组合去重

3. **排序规则**：
   - 主排序：价格从低到高
   - 次排序：销量从高到低

4. **性价比计算**：
   - 性价比得分 = 店铺评分 × log(销量 + 1) / 价格
   - 标注Top 3高性价比商品

## 6. 可视化方案

| 图表类型 | 用途 | 实现方式 |
|----------|------|----------|
| 横向对比表格 | 展示商品详细信息对比 | HTML/CSS 表格 |
| 价格分布直方图 | 展示价格区间分布 | Matplotlib |
| 价格-销量散点图 | 展示价格与销量关系 | Matplotlib |
| 性价比雷达图 | 展示商品综合评分 | Matplotlib |

## 7. 技术栈

- **语言**：Python 3.10+
- **Web框架**：Flask 2.0+
- **HTTP请求**：requests
- **HTML解析**：BeautifulSoup4
- **数据处理**：pandas
- **可视化**：matplotlib
- **模板引擎**：Jinja2
- **命令行**：argparse
